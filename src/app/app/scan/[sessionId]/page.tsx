/**
 * Active Scan Page — Chat interface for active scan session
 */

'use client';

import ChatContainer from '@/components/cde/chat/ChatContainer';

export default function ActiveScanPage() {
  return <ChatContainer />;
}
