/**
 * Kriya Pain Diagnostics — Scan route.
 * Thin client page that mounts the chat orchestrator.
 */

import DiagnosticsChat from '@/components/diagnostics/DiagnosticsChat';

export default function ScanPage() {
  return <DiagnosticsChat />;
}
